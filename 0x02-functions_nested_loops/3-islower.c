#include "main.h"

/**
 *main - check if c is lowercase
 *
 *@c: int checked
 *Return: 1 if lower, 0 if otherwise
 */

int _islower(int c)
{
	if (c = lowercase)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
